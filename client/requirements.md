## Packages
react-webcam | For capturing face images from the user's camera
react-media-recorder | For recording voice audio from the microphone
framer-motion | For complex animations like the biometric scanning effect
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  mono: ["var(--font-mono)"],
  sans: ["var(--font-sans)"],
}
